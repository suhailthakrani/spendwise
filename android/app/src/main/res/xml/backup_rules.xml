<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Defense in depth if backup is ever re-enabled. -->
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="FlutterSecureStorage" />
</full-backup-content>
